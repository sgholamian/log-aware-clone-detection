//,temp,sample_3117.java,2,17,temp,sample_3116.java,2,17
//,2
public class xxx {
public void dummy_method(){
if (!isArray && !(data instanceof List)) {
if (!warned) {
warned = true;
}
return data;
}
int listSize = (isArray ? ((Object[]) data).length : ((List<Object>) data) .size());
MyField f = (MyField) fieldRef;
if (fields.size() != listSize && !warned) {
warned = true;


log.info("ignoring similar errors");
}
}

};