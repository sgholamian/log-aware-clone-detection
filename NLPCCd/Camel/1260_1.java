//,temp,sample_2984.java,2,16,temp,sample_2983.java,2,16
//,3
public class xxx {
public void dummy_method(){
for (int i = 0; i < files; i++) {
Writer out = IOHelper.buffered(new FileWriter(new File("target/files", "data" + i)));
for (int j = 0; j < rows; j++) {
out.write(DATA);
}
out.close();
}
StopWatch watch = new StopWatch();
context.startRoute("foo");
boolean matches = notify.matches(3, TimeUnit.MINUTES);


log.info("should process all files");
}

};