//,temp,sample_3624.java,2,11,temp,sample_2741.java,2,9
//,3
public class xxx {
protected void restoreTimeoutMapFromAggregationRepository() throws Exception {
Set<String> keys = aggregationRepository.getKeys();
if (keys == null || keys.isEmpty()) {
return;
}
StopWatch watch = new StopWatch();


log.info("starting restoring completiontimeout for existing exchanges from the aggregation repository");
}

};