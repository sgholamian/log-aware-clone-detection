//,temp,sample_356.java,2,14,temp,sample_6217.java,2,14
//,2
public class xxx {
private DirectConsumer awaitConsumer() throws InterruptedException {
DirectConsumer answer = null;
StopWatch watch = new StopWatch();
boolean done = false;
while (!done) {
Thread.sleep(500);
if (LOG.isDebugEnabled()) {


log.info("waited for consumer to be ready");
}
}
}

};